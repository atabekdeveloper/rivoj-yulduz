export type TSupportItem = {
  id: number;
  name: string;
  phone: string;
  description: string;
};
export type TSupportChange = {
  id?: number;
  name: string;
  phone: string;
  description: string;
};
