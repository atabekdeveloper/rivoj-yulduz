import React from 'react';

import './lazy.scss';

const ReactSuspenseLoading: React.FC = () => (
  <div className="lazy-container">
    <span className="loader" />
  </div>
);

export { ReactSuspenseLoading };
